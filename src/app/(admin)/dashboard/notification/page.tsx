import React from 'react';
import Notifications from '@/app/(admin)/dashboard/notification/Notifications';

const page = () => {
  return (
    <div>
        <Notifications />
    </div>
  )
}

export default page